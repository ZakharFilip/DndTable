/** Extensible id for drawable shape variants (maps to appearance.shape for now). */
export type ShapeVariantId = "rectangle" | "ellipse";

export const SHAPE_VARIANT_IDS: ShapeVariantId[] = ["rectangle", "ellipse"];
