# Hierarchy

Задача: дерево объектов на сцене (группы/родители).

Сделать:
- Список, drag-n-drop порядка/вложенности (позже)
- Выбор объекта

Готово, когда:
- Выбор объекта отображается и в Inspector/Canvas

