# Assets

Задача: загрузка и выдача файлов (локальное хранилище).

API:
- POST `/assets` (multipart)
- GET `/assets/:id`

Сделать:
- Адаптер локальной папки (`ASSETS_DIR`)
- Метаданные, привязка к party/scene, ACL

Готово, когда:
- Можно загрузить файл и использовать его в Shape (image)

