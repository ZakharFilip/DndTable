# Development — copy to frontend/.env before npm run build
# Same-origin in production: leave empty or omit both variables (see frontend.env.production.example)

VITE_API_BASE=http://localhost:4000
VITE_SOCKET_URL=http://localhost:4000
